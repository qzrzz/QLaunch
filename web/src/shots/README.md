# Marketing screenshots

Put marketing screenshots / demos here, then import in `content.ts`, e.g.:

```ts
import heroShot from "./shots/s1.png";
// ...
cards: [{ style: "center", image: heroShot }],
```

Source files may be PNG/JPEG; production build converts them to lossless WebP.
